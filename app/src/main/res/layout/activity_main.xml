<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#121212">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipChildren="false"
        android:clipToPadding="false">

        <!-- Primary Navigation (Bottom) -->
        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottomNavigation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:background="@drawable/bottom_nav_background"
            android:paddingStart="0dp"
            android:paddingEnd="0dp"
            android:paddingTop="0dp"
            android:paddingBottom="0dp"
            app:itemIconSize="24dp"
            app:itemIconTint="@color/bottom_nav_icon_color"
            app:itemTextColor="@color/bottom_nav_text_color"
            app:itemActiveIndicatorStyle="@style/BottomNavActiveIndicatorStyle"
            app:itemRippleColor="@color/bottom_nav_ripple_color"
            app:labelVisibilityMode="labeled"
            app:itemPaddingTop="2dp"
            app:itemPaddingBottom="4dp"
            app:menu="@menu/bottom_nav_menu" />

        <!-- Fragment Container (Content) -->
        <!-- Placed above bottomNavigation so it doesn't get hidden by it -->
        <FrameLayout
            android:id="@+id/fragment_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_above="@id/bottomNavigation" />

        <!-- Player Control Bar (Mini Player) - Overlay -->
        <!-- Floating above the content, anchored to bottom nav -->
        <include
            android:id="@+id/miniPlayerContainer"
            layout="@layout/layout_mini_player"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_above="@id/bottomNavigation"
            android:visibility="gone" />

    </RelativeLayout>

</androidx.drawerlayout.widget.DrawerLayout>
